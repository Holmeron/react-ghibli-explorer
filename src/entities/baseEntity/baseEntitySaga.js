import { put, call } from 'redux-saga/effects';
import { ghibliApi } from '../../Api/GhibliApi';
import * as types from '../../actions/rootActionsTypes';

export function* findEntity(entity,id) {
  try {

  } catch (error) {
  }
}
